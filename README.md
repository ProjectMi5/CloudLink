# CloudLink Showcase Mi5

## Installation

`npm install`

Start the app in linux with the forever daemon:

`./start.sh`

## Development

Rebuild the documentation:

`npm run-script docs`

Run the Tests:

`npm test`
